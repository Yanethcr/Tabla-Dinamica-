//se crea una interface que tendra un objeto de tipo usuario

export interface Usuario {
    id: number;
    nombre: string;
    correo: string;
    rolSistema: string;
    fechaRegistro: string;
}